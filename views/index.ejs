<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>City Temperature</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <div class="container">
      <fieldset>
        <form action="/" method="post">
          <input name="city" type="text" class="ghost-input center" placeholder="Enter a City" required>
          <input type="submit" class="ghost-button" value="Get Weather">
        </form>
        <% if(locals.weather !== null) { %>
            <p class="center"><%= locals.weather %></p>
        <% } %>

        <% if(locals.error !== null) { %>
            <p class="center"><%= locals.error %></p>
        <% } %>
      </fieldset>
    </div>
  </body>
</html>